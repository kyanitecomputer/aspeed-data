[workspace]
members = [
    "aspeed-data-gen",
]
# aspeed-pac is its own standalone workspace (no external path deps).
# Check it with: cargo check --manifest-path aspeed-pac/Cargo.toml ...
resolver = "2"

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
panic = "abort"
